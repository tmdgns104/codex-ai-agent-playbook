# V8_1-CAP-006 - Codex Discovery Bridge / Pre-session Activation Prototype

상태: **COMPLETE - VERIFIED**

선행 Task:

- `V8_1-CAP-001 - COMPLETE - VERIFIED`
- `V8_1-CAP-002 - COMPLETE - VERIFIED`
- `V8_1-CAP-002A - COMPLETE - VERIFIED`
- `V8_1-CAP-003 - COMPLETE - VERIFIED`
- `V8_1-CAP-004 - COMPLETE - VERIFIED`
- `V8_1-CAP-005 - COMPLETE - VERIFIED`

## 목적

CAP-005에서 검증한 선택 Skill staging을 실제 Codex Skill discovery 규칙과 연결하는 pre-session bridge를 구현합니다.

이번 Task는 **새 Codex 세션 시작 전 준비/검증/cleanup prototype**입니다. 실행 중 세션 hot-reload를 가정하지 않습니다.

## 확인된 Codex 동작

OpenAI Codex 현재 구현 기준:

- 사용자 Skill root에 `~/.agents/skills`가 포함됨
- Git/project root부터 현재 `cwd`까지 각 디렉터리의 `.agents/skills`를 discovery root로 탐색
- Skill이 구성되면 세션 초기 input에 Skill metadata가 포함됨
- Codex CLI는 `-C/--cd <DIR>`로 working root를 지정할 수 있음
- `--add-dir <DIR>`로 추가 writable directory를 연결할 수 있음
- TUI `/skills`에서 현재 사용 가능한 Skill을 확인할 수 있음

따라서 전역 `~/.agents/skills`를 변경하지 않고 repo 하위 session 전용 cwd에 `.agents/skills`를 만들 수 있습니다.

## 구현

추가:

```text
harness/activation/discovery_bridge.py
harness/activation/test_discovery_bridge.py
```

Bridge 구조:

```text
<repo>/
  .agents/skills/                    # 기존 Core Skill, 수정 안 함
  .playbook-runtime/
    <session>/
      activation.json
      bridge.json
      cwd/
        .agents/
          skills/
            security-review/
            testing/
            root-cause-debugging/
```

CAP-005 materializer를 재사용한 뒤 managed `skills/`를 `cwd/.agents/skills/`로 이동하고 activation manifest destination을 갱신합니다. SHA256 status 검증은 그대로 재사용합니다.

## CLI

Prepare:

```cmd
python harness\activation\discovery_bridge.py prepare --root . --task "<task>" --target .playbook-runtime --session <id>
```

Status:

```cmd
python harness\activation\discovery_bridge.py status --target .playbook-runtime --session <id>
```

Launch command:

```cmd
python harness\activation\discovery_bridge.py command --target .playbook-runtime --session <id>
```

Cleanup:

```cmd
python harness\activation\discovery_bridge.py cleanup --target .playbook-runtime --session <id>
```

## Launch 계약

Bridge는 Codex를 자동 실행하지 않습니다. 다음 argv만 생성합니다.

```text
codex -C <repo>/.playbook-runtime/<session>/cwd --add-dir <repo>
```

## Focused Tests

`harness/activation/test_discovery_bridge.py`

1. trivial -> NO_BRIDGE
2. JWT -> exactly 3 optional Skill
3. `code-review` 미노출
4. CAP-005 integrity 유지
5. launch argv `-C` / `--add-dir`
6. repository Core Skill fingerprint 불변
7. duplicate session FAIL
8. unsafe session FAIL
9. outside-repo target FAIL
10. tamper detection / managed cleanup

## 실제 Windows + Codex Evidence

실제 Task:

```text
JWT 인증 오류를 수정하고 regression test를 실행
```

Bridge prepare:

```text
DISCOVER    security-review
DISCOVER    testing
DISCOVER    root-cause-debugging
COUNT       3
DISCOVERY   true
RESULT      BRIDGE_READY
```

Bridge status:

```text
COUNT       3
INTEGRITY   INTEGRITY_PASS
DISCOVERY   true
RESULT      DISCOVERY_READY
```

새 Codex 세션을 생성된 `codex -C <bridge-cwd> --add-dir <repo>` 명령으로 시작했습니다.

Codex TUI `/skills -> Enable/Disable Skills` 실제 목록에서 다음을 확인했습니다.

```text
root-cause-debugging  present
security-review       present
testing               present
code-review           not exposed
```

기존 Core Skill도 함께 정상적으로 표시되어 repository root Skill 구성이 유지됨을 확인했습니다.

Cleanup:

```text
REMOVED     true
COUNT       3
RESULT      BRIDGE_CLEANED
```

추가 Evidence:

- bridge residue 없음
- discovery bridge focused tests 10 PASS
- Harness Audit PASS
- warnings 0
- STRICT Quality Gate PASS
- exit code 0
- final `git status --short` clean

## Safety

- pre-session only
- hot-reload 주장 없음
- global `~/.agents/skills` write 없음
- repository root `.agents/skills` optional install 없음
- bridge target은 repository 하위만 허용
- Git root 필수
- CAP-006 자체는 Codex 자동 실행 없음
- MCP/Agent/network/credential/external write 없음

## Acceptance Criteria

1. focused tests PASS ✅
2. JWT bridge optional Skill exactly 3 ✅
3. status `DISCOVERY_READY` ✅
4. 실제 Codex `/skills` selected 3 확인 ✅
5. `code-review` 미노출 확인 ✅
6. cleanup PASS ✅
7. Harness Audit PASS ✅
8. STRICT Quality Gate PASS ✅
9. exit 0 ✅
10. working tree clean ✅

## 완료 후 다음 Task

`V8_1-CAP-007 - Discovery Bridge Usability / One-command Pre-session Launcher`
