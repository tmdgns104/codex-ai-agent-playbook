# V8_1-CAP-001 - Capability Registry Baseline

상태: **COMPLETE - VERIFIED**

부모:

- `V8_1_REQUIREMENTS.md`
- `V8_1_ARCHITECTURE.md`
- `V8_1_CAPABILITY_POLICY.md`
- `V8_1_EVALUATION_PLAN.md`

## 목적

V8.1의 첫 구현 단계로 Capability Library의 metadata registry와 deterministic validator를 추가합니다.

이 Task에서는 실제 MCP 연결, Agent spawn, 외부 write, credential 사용을 구현하지 않습니다.

## Allowed

- `capability-library/registry.json`
- `capability-library/sources.json`
- `harness/router/registry.py` 또는 동등한 단일 registry validator 모듈
- registry validator 테스트/fixture
- `harness/security/harness_audit.py`의 registry 검증 연동
- `MANIFEST.txt` 갱신
- V8.1 관련 문서/Task 상태 갱신

## Forbidden

- `.codex/AGENTS.md` 확대
- 기존 V8 Quality Gate 의미 변경
- 실제 MCP config 변경
- MCP 서버 설치
- Agent 자동 실행
- credential/external-write/database-write/destructive capability 활성화
- 기존 7개 Core Skill의 책임을 임의 변경

## Registry 최소 Schema

Capability entry 필수:

- `id`
- `type`
- `summary`
- `domains`
- `triggers`
- `activation`
- `risk`
- `recommended_profile`
- `permissions`
- `context_cost`
- `dependencies`
- `source_id`
- `license`
- `path`

초기 enum 후보:

### type

- `skill`
- `mcp`
- `agent`
- `cli-wrapper`
- `rest-wrapper`

### activation

- `on_demand`
- `conditional`
- `manual`

### risk

- `low`
- `medium`
- `high`
- `critical`

### recommended_profile

- `minimal`
- `standard`
- `strict`

### permissions

허용 값:

- `local_read`
- `local_write`
- `process_exec`
- `network`
- `browser_control`
- `credential_access`
- `external_write`
- `database_write`
- `destructive`
- `production`

## 초기 Registry Capability

P0 metadata 6개:

1. `security-review`
2. `testing`
3. `documentation-lookup`
4. `github-ops`
5. `root-cause-debugging`
6. `code-review`

이 Task에서는 Capability 본문을 반드시 모두 구현할 필요는 없습니다. Registry/validator 검증이 우선입니다.

## Validator 요구사항

최소 검사:

- JSON parse
- top-level schema
- required fields
- unique `id`
- enum validity
- non-empty summary/domains
- permissions enum
- dependencies가 알려진 id를 참조하는지
- `source_id`가 `sources.json`에 존재하는지
- optional capability path가 허용된 Library root 아래인지
- absolute/personal path 금지

## Harness Audit 연동

기존 `harness_audit.py` 결과에 다음 항목을 추가했습니다.

```text
PASS       capability sources
PASS       capability registry
```

Python 실행 중 생성되는 `__pycache__`, `*.pyc`, `*.pyo`는 배포 관리 파일이 아니므로 Audit/MANIFEST 대상에서 제외하고 `.gitignore`로도 무시합니다.

## Acceptance Criteria

1. [x] 정상 registry validator exit 0
2. [x] duplicate id fixture FAIL
3. [x] invalid permission fixture FAIL
4. [x] unknown source fixture FAIL
5. [x] personal absolute path fixture FAIL
6. [x] 기존 Harness Audit 검사 유지
7. [x] `MANIFEST.txt`에 새 managed files 반영
8. [x] 기존 V8 Quality Gate 의미/출력 계약 유지

## 실제 Windows Verification - 2026-08-22

실행 환경:

```text
D:\Codex_Playbook\codex_ai_agent_playbook_kit_v5
branch: v8.1-capability-library
```

### Registry validator

```cmd
python harness\router\registry.py --root .
```

결과:

```text
PASS       capability sources: 1
PASS       capability registry: 6
RESULT     PASS
```

### Focused registry tests

```cmd
python harness\router\test_registry.py
```

결과:

```text
Ran 6 tests
OK
```

6개 테스트에는 정상 case와 duplicate id, invalid permission, unknown source, personal absolute path, unknown dependency negative case가 포함됩니다.

### Harness Audit 1차 실행

1차 실제 실행에서는 다음 문제가 검출되었습니다.

```text
FAIL       managed files missing from MANIFEST: harness/router/__pycache__/registry.cpython-313.pyc
```

원인:

- Python 테스트 실행 시 생성되는 `__pycache__/*.pyc`를 Harness Audit이 배포 managed file로 오인함.

수정:

- Harness Audit에서 `__pycache__`, `*.pyc`, `*.pyo` 제외
- `.gitignore` 추가

### Harness Audit 재검증

```cmd
python harness\security\harness_audit.py --root .
```

결과:

```text
PASS       capability sources
PASS       capability registry
PASS       harness Python syntax
PASS       MANIFEST covers managed files
INFO       warnings: 0
RESULT     PASS
```

### STRICT Quality Gate

```cmd
python harness\quality\quality_gate.py --repo . --profile strict --verify "python harness\security\harness_audit.py --root ."
echo %ERRORLEVEL%
```

결과:

```text
RESULT     PASS
0
```

### Working Tree

```cmd
git status --short
```

출력 없음. Clean 확인.

## 완료 판단

CAP-001은 실제 Windows Evidence 기준 **COMPLETE - VERIFIED**입니다.

초기 실패도 숨기지 않고 원인 수정 후 동일 환경에서 재검증했습니다.

## 다음 Task

`V8_1-CAP-002 - Deterministic Capability Router`
